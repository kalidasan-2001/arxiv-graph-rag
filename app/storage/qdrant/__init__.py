"""Qdrant vector store adapter (prompt 7).

Application code depends only on `VectorRepository` (`repository.py`) and
the DTOs in `models.py` -- `qdrant_client` objects never cross this
package's boundary (CLAUDE.md #15).
"""
